# وثيقة المراجعة والتدقيق الهندسي المعماري لمشروع CSW وتطبيق CSWMobile
**اسم الملف المرجعي:** `CSWMobile_PROJECT_EXPLANATION_REVIEWED.md`  
**تاريخ المراجعة والتدقيق:** 2026-09-05  
**طبيعة الوثيقة:** تدقيق هندسي ناقد وصادق يفصل بين الحقائق المثبتة من الكود، والافتراضات التحليلية، والمعلومات غير المحسومة، ليكون المرجع الأساسي المعتمد قبل البدء في أي برمجة.

---

## 1. مقدمة (Introduction)
مشروع **CSW — Collaborative Student Workspace** هو نظام عمل تعاوني أكاديمي مخصص لفرق العمل الطلابية للعمل المشترك داخل **شبكة محلية (Local Area Network - LAN)**.
تعتمد المنظومة الأصلية على بيئة **C# / .NET** وواجهات **Windows Forms (WinForms)**، وتتكون من خادم مركزي (`ChatServer`) وعميل حاسوب مكتبي (`ChatClient`).

الهدف من هذه الوثيقة هو **التدقيق المعماري الصارم** للتحليل الأولي السابق، وإزالة أي مبالغات تقنية أو افتراضات غير مثبتة، وتحديد ما هو مؤكد وما هو محتمل وما هو مجهول بدقة علمية قبل كتابة أي كود على بيئة Android Native.

---

## 2. الهدف من تطبيق CSWMobile (Objective of CSWMobile)
الهدف هو تصميم وبناء تطبيق هاتف محمول مستقل بنظام **Android Native** باستخدام لغة **Kotlin**:
1. **العمل كعميل متكافئ:** تمكين الطالب من الانضمام إلى مساحة العمل التعاونية وتبادل الرسائل والملفات مع زملائه المستخدمين لتطبيق Windows.
2. **الاتصال بالخادم القائم فقط:** التوصيل المباشر بخادم `ChatServer` القائم دون إجراء أي تعديلات على كود الخادم، ودون استبداله.
3. **الالتزام بعدم إنشاء خدمات وسيطة:** حظر استخدام خوادم وسيطة أو خدمات سحابية (مثل Firebase أو Supabase) أو توليد بيانات وهمية (Mock Data).
4. **التوافق الحقيقي مع البروتوكول:** ضبط طبقة الشبكة في أندرويد لتتوافق تماماً مع البروتوكول الفعلي للخادم بعد التحقق المباشر من قواعد نقل البيانات.

---

## 3. البنية العامة للنظام (System Architecture)
يعتمد النظام معمارية **عميل-خادم (Client-Server)** مركزية في الشبكة المحلية:

```text
                                  ┌─────────────────────────────────────────┐
                                  │               ChatServer                │
                                  │           (C# / .NET / WinForms)        │
                                  │  ├─ TCP Listener                        │
                                  │  ├─ SignalR Hubs (محتمل/غير مكتمل)      │
                                  │  └─ CollaborationStore (In-Memory/File) │
                                  └────────────────────┬────────────────────┘
                                                       │
                           ┌───────────────────────────┴───────────────────────────┐
                           │                                                       │
                           ▼                                                       ▼
            ┌───────────────────────────────┐                       ┌───────────────────────────────┐
            │          ChatClient           │                       │           CSWMobile           │
            │   (Windows Desktop Client)    │                       │       (Android Native)        │
            │  ├─ TCP Socket Connection     │                       │  ├─ Native TCP Socket Client  │
            │  ├─ WinForms UI               │                       │  ├─ Jetpack Compose UI        │
            │  └─ ChatProtocol Handling     │                       │  └─ ChatProtocol Parser       │
            └───────────────────────────────┘                       └───────────────────────────────┘
```

* **طبيعة المعمارية:** لا يوجد أي اتصال مباشر Peer-to-Peer بين الهواتف والحواسيب؛ كافة العمليات تمر حتماً عبر `ChatServer`.

---

## 4. مكونات الخادم (Server Components)

| المكون | وظيفته المتوقعة | حالة التوثيق في الكود | درجة التأكيد |
| :--- | :--- | :--- | :--- |
| `ChatServer.cs` | إدارة `TcpListener` وقبول اتصالات العملاء ومتابعة جلساتهم. | مذكور في التقارير السابقة كعمود فقري للاتصال. | **مؤكد كوجود عام / غير مؤكد تفاصيل الدوال** |
| `CollaborationStore.cs` | حفظ الرسائل وقوائم المستخدمين والملفات في الذاكرة أو القرص. | مذكور كمسؤول عن Snapshot وسجل الجلسة. | **مؤكد كوجود عام / غير مؤكدة بنية التخزين** |
| `CollaborationHub.cs` | بث إشعارات وتحديثات فورية عبر SignalR. | مذكور كـ Hub للأحداث الفورية. | **غير مؤكدة مساراته ودواله الفعلية** |
| `CallHub.cs` | إدارة إشارات الاتصال الصوتي/المرئي (Signaling). | موجود كملف في المشروع الأصلي. | **مؤكد كملف إشارات / غير مؤكد كمحرك مكالمات** |
| `ServerForm.cs` | واجهة تحكم ومراقبة للـ Server في بيئة WinForms. | واجهة تشغيل الخادم وعرض المنافذ. | **مؤكد كواجهة تشغيل** |
| `Program.cs` | نقطة الدخول لتشغيل الخادم وتكوين الخدمات. | نقطة تشغيل .NET. | **مؤكد كوجود قياسي** |

---

## 5. مكونات العميل الأصلي (Windows Client Components)

| المكون | الوظيفة المتوقعة | الحالة والتقييم الهندسي |
| :--- | :--- | :--- |
| `ClientForm.cs` | شاشة تسجيل الدخول وإدخال عنوان IP والمنفذ. | مؤكد وجوده كبوابة الدخول للعميل المكتبي. |
| `TeamWorkspaceForm.cs` | واجهة مساحة العمل الرئيسية (الشات، قائمة الزملاء، الملفات). | مؤكد وجوده كواجهة رئيسية للطلاب. |
| `SignalRRealtimeClient.cs` | فئة إدارة اتصال SignalR بالخادم. | غير مؤكد ما إذا كان مستخدماً في كل الأحداث أو كقناة إشعار ثانوية. |
| `ChatProtocol.cs` | فئة قراءة وتشفير الحزم الثنائية. | **المكون الأكثر حساسية وأهمية، وتفاصيل بايتاته تحتاج فحصاً مباشراً.** |

---

## 6. المنافذ والاتصالات (Ports and Networking)

### 6.1 قاعدة عنوان IP في Android
* **حقيقة هندسية حاسمة:** لا يجوز إطلاقاً استخدام `localhost` أو `127.0.0.1` في تطبيق أندرويد للاتصال بالخادم، لأنها تشير إلى الهاتف المحمول نفسه.
* **المسار المعتمد:** يجب إدخال عنوان IPv4 الخاص بحاسوب Windows في الشبكة المحلية (مثال: `192.168.1.x`).

### 6.2 جدول تدقيق المنافذ

| المعلومة | القيمة المذكورة في التحليل السابق | هل هي مؤكدة من الكود؟ | الدليل المتاح | ما المطلوب لتأكيدها قطعيًا |
| :--- | :--- | :--- | :--- | :--- |
| **منفذ TCP الأساسي** | `5000` | **غير مؤكدة** | وردت في تقرير سابق فقط كقيمة شائعة. | فحص `appsettings.json` أو كود `ServerForm.cs` أو `ChatServer.cs`. |
| **منفذ SignalR** | `5001` أو `PrimaryPort + 1` | **غير مؤكدة** | فرضية مبنية على مشاريع مشابهة. | فحص كود `Program.cs` أو إعدادات Kestrel/ASP.NET. |
| **مسار SignalR URL** | `http://<IP>:<Port>/collaborationHub` | **غير مؤكدة** | مسار مقترح بالتحليل السابق دون دليل نصي. | فحص مسار `app.MapHub<CollaborationHub>("/...")`. |
| **منفذ استدعاء الكاميرا/الفيديو** | لا يوجد منفذ منفصل | **استنتاج منطقي** | تسجيل الفيديو يتم محلياً بواسطة كاميرا الجهاز. | فحص كود `CameraRecordingForm.cs`. |

> **القرار الهندسي بخصوص المنافذ:**  
> لا يجوز تثبيت Port `5000` كقيمة ثابتة لا تتغير (Hardcoded). يجب أن توفر واجهة Android حقولاً مرنة لإدخال عنوان IP ورقم المنفذ مع وضع القيمة المقترحة كخيار افتراضي قابل للتعديل الفوري.

---

## 7. تدقيق ChatProtocol وفحص البايتات (ChatProtocol Deep Audit)

في التحليل السابق، تم افتراض بنية الحزمة كالتالي:
`[4 Bytes MessageType] + [4 Bytes PayloadLength] + [Payload JSON/Binary]` بترميز Little-Endian.

### 7.1 تدقيق هذا الافتراض هندسيًا:
هذه البنية **غير مؤكدة من الكود المصدري الأصلي**، وتعتبر **فرضية هندسية محتملة** للأسباب التالية:
1. **طريقة كتابة الأعداد في C#:**
   - إذا كان الكود يستخدم `BinaryWriter.Write(int)`، فإنه يكتب 4 بايتات بنظام Little-Endian.
   - إذا كان الكود يكتب الـ `enum` مباشرة: `BinaryWriter.Write(MessageType)`، فإن حجم البايتات يعتمد على النوع الأساسي للـ enum (قد يكون `int` = 4 بايت، أو `byte` = بايت واحد، أو `short` = 2 بايت).
   - إذا كان الكود يستخدم `BitConverter.GetBytes()`، فإن الترتيب يعتمد على معمارية المعالج (غالباً Little-Endian في معالجات x86/ARM).
   - إذا كان الكود يستخدم `BinaryPrimitives` في .NET 8، فقد يكون الترتيب Big-Endian أو Little-Endian بحسب الدالة المستدعاة.
2. **طريقة تحديد طول النص:**
   - دالة `BinaryWriter.Write(string)` في C# لا تكتب الطول كـ 4 بايتات صريحة، بل تكتب طول النص كـ **7-bit Encoded Integer** متبوعاً بحروف UTF-8.
   - بينما كتابة `BinaryWriter.Write(payloadBytes.Length)` متبوعة بـ `Write(payloadBytes)` هي التي تولد 4 بايتات صريحة لطول الحمولة.
3. **طريقة نقل الملفات:**
   - غير مؤكد ما إذا كانت الملفات ترسل برأس خاص يحتوي على اسم الملف وحجمه، أم ترسل كـ JSON يحوي مصفوفة بايتات Base64، أم كتدفق ثنائي منفصل بعد حزمة تنبيه.

### 7.2 التوصيف الدقيق لـ ChatProtocol في هذه المرحلة:
> **البنية التالية مقترحة بناءً على التقرير السابق والأنماط المعمارية الشائعة في مشاريع WinForms المشابهة، لكنها تحتاج إلى تأكيد مباشر من كود `ChatProtocol.cs` الأصلي قبل اعتمادها نهائياً:**
> - رأس الحزمة (Header): كود نوع العملية (النوع والحجم يحتاجان تأكيداً) + طول الحمولة.
> - الحمولة (Payload): نص JSON مشفر بـ UTF-8 أو تدفق بايتات.

---

## 8. تدقيق النماذج والـ Enums (Models & Enums Audit)

### 8.1 جدول تدقيق النماذج (Models / DTOs)

| النموذج (Model) | الخصائص المؤكدة كفكرة عامة | الخصائص غير المؤكدة قطعيًا | الاستخدام المتوقع في النظام |
| :--- | :--- | :--- | :--- |
| **`AuthPayload`** | اسم المستخدم (Username). | كلمة المرور (Password)، نوع العميل (ClientType)، اسم الجهاز (DeviceName). | إرسال بيانات المستخدم عند فتح الاتصال لأول مرة. |
| **`SendMessagePayload`**| اسم المرسل، نص الرسالة. | معرف الرسالة الفريد (`MessageId` / GUID)، التوقيت (`Timestamp`)، الغرفة المستهدفة (`TargetRoom`). | تبادل المحادثات النصية بين الطلاب. |
| **`CollaborationSnapshot`**| قائمة المستخدمين المتصلين، الرسائل الأخيرة. | الحقول الدقيقة وتسمياتها: هل هي `ConnectedUsers` أم `Users`؟ وهل الملفات مدرجة بها؟ | مزامنة حالة مساحة العمل فور تسجيل الدخول. |
| **`CollaborationCommand`**| نوع الأمر، اسم المستخدم. | الحقول الإضافية (`ExtraData` أو المعاملات المصاحبة). | إرسال إشعارات التواجد، الكتابة، والتنبيهات. |
| **`FileMetadataPayload`**| اسم الملف، حجم الملف. | هل هو نموذج مستقل أم جزء من حزمة الملف أم مرسل عبر SignalR؟ | إشعار العملاء بتفاصيل الملف المرفوع. |

### 8.2 جدول تدقيق التعداد (Enums)

| التعداد (Enum) | القيمة المفترضة سابقاً | الاسم المذكور | هل الرقم مؤكد من الكود؟ | الحقيقة الهندسية |
| :--- | :--- | :--- | :--- | :--- |
| `MessageType` | 1 | `Auth` / `AuthRequest` | **غير مؤكد إطلاقاً** | الرقم مجرد تخمين تحليلي. لا يجوز الجزم به دون فتح ملف `ChatProtocol.cs`. |
| `MessageType` | 2 | `AuthSuccess` | **غير مؤكد إطلاقاً** | لم يرد دليل صريح على رقمه. |
| `MessageType` | 3 | `AuthFailed` | **غير مؤكد إطلاقاً** | لم يرد دليل صريح على رقمه. |
| `MessageType` | 4 | `ChatMessage` | **غير مؤكد إطلاقاً** | لم يرد دليل صريح على رقمه. |
| `MessageType` | 5 | `SendFile` | **غير مؤكد إطلاقاً** | لم يرد دليل صريح على رقمه. |
| `MessageType` | 8 | `CollaborationSnapshot` | **غير مؤكد إطلاقاً** | لم يرد دليل صريح على رقمه. |
| `CollaborationCommandType`| - | `Join`, `Leave`, `Typing` | **الأسماء محتملة / القيم مجهولة** | يحتاج فحص التعداد في `CollaborationModels.cs`. |

> **قاعدة صارمة:** ممنوع اختراع أرقام للـ Enums وادعاء أنها مطابقة للخادم. أي كود يتم بناؤه دون مطابقة الأرقام الفعلية من ملفات C# سيفشل في التخاطب مع الخادم.

---

## 9. تدقيق دورة المصادقة (Authentication Lifecycle Audit)

### 9.1 المسار المفترض سابقاً:
1. فتح مقبس TCP.
2. إرسال `AuthPayload`.
3. استقبال `CollaborationSnapshot`.
4. إنشاء اتصال SignalR.

### 9.2 التقييم الهندسي والأسئلة غير المحسومة:
* **هل المصادقة تتم عبر TCP أم SignalR؟**  
  المعلومات تشير إلى أن TCP هو القناة الرئيسية، لكن لم يثبت هل المصادقة تتم بمجرد إرسال حزمة عادية، أم بطلب مصافحة خاص (Handshake Packet)، أم أن اسم المستخدم يرسل كنص بسيط عند بداية فتح الاتصال.
* **هل يتم إرسال Snapshot تلقائياً بعد المصادقة؟**  
  احتمال كبير منطقياً، ولكن قد يتطلب أن يرسل العميل طلباً صريحاً مثل `GetSnapshot` أو `Refresh`.
* **هل يوجد Token أو Session ID؟**  
  غير مؤكد. قد يعتمد الخادم على مجرد مقبس العميل (`Socket Handle` أو `IP:Port`) لربط الجلسة دون أي رموز مصادقة Tokens.
* **ماذا يحدث عند فشل المصادقة؟**  
  غير مؤكد هل يرسل الخادم رسالة خطأ صريحة مثل `AuthFailed` ثم يغلق المقبس، أم يغلق المقبس مباشرة دون رد.

---

## 10. تدقيق دورة الرسائل (Messaging Lifecycle Audit)

### 10.1 المسار المؤكد كمنطق عام:
1. العميل يرسل الرسالة إلى `ChatServer`.
2. الخادم يستقبل الرسالة ويوثقها.
3. الخادم يعيد توزيع الرسالة (Broadcast) على بقية العملاء المتصلين.

### 10.2 المسار المحتمل الذي يحتاج إلى فحص الكود:
* **أسماء الدوال والأحداث:** دوال مثل `SendMessageAsync` أو `WriteMessage` أو `BroadcastMessage` هي مجرد أسماء افتراضية مستنتجة ولم يتم استخراجها من كود الخادم الفعلي.
* **طريقة استرجاع الـ Echo:** هل الخادم يعيد إرسال الرسالة إلى نفس العميل المرسل كتأكيد (Echo/Ack)، أم يفترض أن العميل يعرض رسالته ذاتياً في واجهته بمجرد الإرسال؟ هذا التفصيل غير محسوم ويتطلب تجربة حية أو قراءة الكود.

---

## 11. تدقيق دورة الملفات والصور (File & Image Transfer Audit)

### 11.1 تدقيق أسلوب النقل:
في التحليل السابق تم افتراض أن الملف يرسل عبر:
`[Header] + [Metadata Length] + [Metadata JSON] + [Raw Binary Chunks]`

* **الحقيقة الهندسية:**  
  طريقة نقل الملفات الحقيقية **غير محسومة قطعيًا**.
* **الاحتمالات الممكنة في مشاريع C# المماثلة:**
  1. **الاحتمال الأول (Metadata ثم Stream):** إرسال حزمة صغيرة تحتوي على اسم وحجم الملف، متبوعة بضخ بايتات الملف مباشرة في المقبس.
  2. **الاحتمال الثاني (حزم مجزأة Chunked Packets):** تقسيم الملف إلى حزم متعددة، كل حزمة لها رأس يحدد رقم الجزء وحجمه حتى يكتمل الملف.
  3. **الاحتمال الثالث (Base64 داخل رسالة عادية):** تحويل الملف الصغير أو الصورة إلى نص Base64 وإرسالها كحقل ضمن رسالة JSON عادية (شائع في المشاريع الطلابية البسيطة للملفات الصغيرة والصور).
  4. **الاحتمال الرابع (منفذ أو بروتوكول منفصل):** استخدام مسار تحميل منفصل (مثل HTTP POST إذا كان ASP.NET Kestrel مفعلًا).

> **الخلاصة:** لا يجوز الجزم بأن نقل الملفات يتم بأسلوب Raw Binary Stream مستقل دون فحص كود استقبال الملفات في `ChatServer.cs`.

---

## 12. تدقيق تقنية SignalR والـ Hubs (SignalR Deep Audit)

| السؤال التقني | الإجابة المدققة هندسيًا |
| :--- | :--- |
| **هل SignalR مستخدم في الخادم؟** | نعم، تشير أسماء الملفات (`CollaborationHub.cs`, `CallHub.cs`) إلى إدراج مكتبة SignalR في مشروع الخادم. |
| **هل SignalR مستخدم في العميل المكتبي؟** | نعم، وجود `SignalRRealtimeClient.cs` يشير إلى وجود عميل SignalR في Windows. |
| **ما أسماء الـ Hubs ومساراتها؟** | الأسماء الظاهرة هي `CollaborationHub` و `CallHub`. مسارات التوجيه الدقيقة (Route URLs) غير مؤكدة وتحتاج مراجعة `Program.cs`. |
| **ما أسماء Methods والأحداث؟** | **غير معروفة تماماً** حالياً. |
| **هل الرسائل تمر عبر SignalR أم TCP؟** | غير محسوم. هل TCP مخصص للملفات فقط والرسائل تمر عبر SignalR؟ أم أن TCP ينقل كل شيء و SignalR للإشعارات فقط؟ هذا السؤال جوهري ويحتاج فحصاً. |
| **هل CallHub يمثل مكالمات مكتملة؟** | **لا.** CallHub هو مجرد موزع رسائل إشارات (Signaling) لتبادل طلبات الاتصال والقبول والرفض. |

---

## 13. تدقيق المكالمات الصوتية والمرئية (Voice & Video Calls Status)
* **الحقيقة القاطعة:**  
  **لا توجد أي مكالمات صوتية أو مرئية حية تعمل في النظام الحالي.**
* **الدليل الهندسي:**  
  - لا توجد بنية وسائط حقيقية (WebRTC PeerConnection, MediaStream, STUN/TURN, أو RTP Socket).
  - كلاس `CallHub.cs` لا يتعدى كونه فئة SignalR لتبادل رسائل نصية أو إشعارية بين جهازين.
* **الموقف الملزم لتطبيق Android:**  
  يُمنع منعاً باتاً ادعاء دعم مكالمات الفيديو المباشرة في واجهات أندرويد أو أمام لجنة المناقشة.

---

## 14. تدقيق تسجيل الفيديو المحلي (Local Video Recording Status)
* **الحقيقة الهندسية:**  
  - نافذة `CameraRecordingForm` واستخدام مكتبة مثل `OpenCvSharp` في مشروع Windows هو لتشغيل الكاميرا محلياً وتسجيل مقطع وحفظه كملف على القرص الصلب.
  - لا علاقة لهذا بالبث المباشر (Live Video Streaming).
  - إرسال الفيديو المسجل يتم معاملته كملف عادي مرفق.

---

## 15. تدقيق تقنية التعرف الضوئي على الحروف (OCR Status)
* **الحقيقة الهندسية:**  
  - **ميزة الـ OCR غير موجودة على الإطلاق** في ملفات الخادم أو عميل سطح المكتب.
  - لا توجد أي مكتبات مسؤولة عن التعرف الضوئي (مثل Tesseract OCR أو ML Kit).
  - تم استبعاد هذه الميزة تماماً من النطاق البرمجي لتطبيق أندرويد لعدم وجود سند لها في الخادم.

---

## 16. حالة بيئة Android الحالية (Current Android Status)

> **[تصحيح وإقرار رسمي]:**  
> بيئة Android مجهزة من حيث البناء (Gradle, JDK 21, Kotlin, Compose)، **لكن تطبيق CSWMobile نفسه لم يتم تنفيذه أو اختباره مع الخادم بعد.**  
> لا يوجد كود اتصال فعلي تم التحقق من عمله وتوافقه مع الخادم الحقيقي حتى هذه اللحظة، وأي واجهات أو ملفات تم إنشاؤها سابقاً هي قوالب أولية غير متصلة بالخادم.

---

## 17. جدول التوافق الهندسي الشامل (Engineered Compatibility Matrix)

| الميزة | الحالة في خادم C# | الحالة في أندرويد حالياً | طبيعة التوافق الممكنة |
| :--- | :--- | :--- | :--- |
| **الاتصال الشبكي** | `TcpListener` قائم | بيئة جاهزة / الكود لم يختبر | استخدام `Socket` أصلي عبر Kotlin Coroutines. |
| **المصادقة** | كود مصادقة موجود | غير منفذ مع الخادم | يتطلب مطابقة حقول `AuthPayload` بدقة. |
| **الرسائل الفورية** | مدعومة | غير منفذة مع الخادم | تتطلب معرفة نوع ورقم حزمة الشات. |
| **نقل الملفات** | مدعوم في الخادم | غير منفذ مع الخادم | يتطلب تأكيد هل هو Stream أم Chunked أم Base64. |
| **عرض الصور** | تعامل كملفات | غير منفذة مع الخادم | جاهزة من جانب أندرويد عبر مكتبة Coil بمجرد وصول الملف. |
| **التواجد (Presence)**| متاح عبر رصد إغلاق المقبس | غير منفذ مع الخادم | ربط دورة حياة التطبيق بإغلاق المقبس تلقائياً. |
| **مكالمات الفيديو**| إشارات فقط (غير مكتملة) | مستبعدة وغير مدعومة | غير مدعومة هندسياً في المنظومة كاملة. |
| **التعرف الضوئي OCR**| غير موجود | غير مدعوم | مستبعد تماماً. |

---

## 18. الحقائق المؤكدة (Confirmed Facts)
1. المنظومة مبنية لتعمل محلياً في شبكة LAN / Wi-Fi عبر بروتوكولات حاسوبية قياسية.
2. الخادم الأصلي مبني بلغة C# وبيئة .NET وواجهات Windows Forms.
3. يوجد اعتماد على المقابس الشبكية (TCP Sockets) كقناة تواصل رئيسية.
4. يوجد استخدام لـ SignalR داخل المشروع ولكن وظيفته التفصيلية غير مؤكدة بدقة.
5. لا توجد خدمات سحابية خارجية (Firebase / Supabase) في معمارية النظام.
6. لا توجد مكالمات فيديو مباشرة ولا محرك OCR.
7. بيئة بناء أندرويد تعمل بنجاح بـ Gradle 9.3.1 و Kotlin 2.2 و Compose.

---

## 19. المعلومات غير المؤكدة (Unconfirmed Information)
1. **أرقام المنافذ الفعلية الافتراضية** (هل منفذ TCP هو 5000؟ وهل منفذ SignalR هو 5001؟).
2. **بنية هيدر حزمة ChatProtocol** (هل هو 8 بايت: 4 للنوع + 4 للطول؟).
3. **طريقة كتابة البايتات في C#** (هل تستخدم Little-Endian صراحة عبر `BinaryWriter`، وما هو نوع حقل enum المكتوب؟).
4. **القيم العددية لـ `MessageType` و `CollaborationCommandType`**.
5. **الأسماء الحرفية لحقول JSON** (هل تبدأ بـ PascalCase مثل `Username` أم camelCase مثل `username`؟).
6. **طريقة تدفق الملفات والصور** (هل هي Metadata منفصلة متبوعة بـ Stream أم حزم مجزأة أم Base64؟).
7. **مسارات SignalR Hubs وقائمة الدوال المعرفة فيها**.

---

## 20. المعلومات المفقودة تماماً (Missing Information)
1. النص المصدري الكامل لملف `ChatProtocol.cs`.
2. النص المصدري لدوال الاستقبال والإرسال في `ChatServer.cs`.
3. ملف إعدادات الخادم `appsettings.json` لمعرفة المنافذ ونقاط النهاية المعرفة رسمياً.
4. كود `CollaborationHub.cs` و `CallHub.cs` لمعرفة أسماء الدوال وأحداث البث.

---

## 21. المخاطر الهندسية الحقيقية (Engineering Risks)
1. **خطر انهيار قراءة الحزم (Protocol Framing Mismatch):** إذا كان الخادم يكتب الهيدر بطول مختلف (مثلاً 2 بايت للـ Type أو 8 بايت للطول `long`)، فإن عميل أندرويد سينهار فوراً عند القراءة أو يقرأ أرقاماً غير صحيحة تؤدي لـ `OutOfMemoryError`.
2. **خطر عدم تطابق قيم الـ Enum:** إذا أرسل أندرويد الرقم 1 لطلب المصادقة وكان الخادم يتوقع الرقم 0 أو 10، سيتجاهل الخادم الرسالة أو يفصل الاتصال فوراً.
3. **خطر تجزئة الحزم عبر Wi-Fi (Packet Fragmentation):** عدم قراءة كامل الحزمة عبر حلقة استلام دقيقة سيؤدي لقص نصوص الـ JSON وتلف الملفات المرفوعة.
4. **خطر حظر الشبكة على الـ Main Thread في أندرويد:** تنفيذ أي عملية مقبس على خيط الواجهة يسبب `NetworkOnMainThreadException` ويغلق التطبيق بالقوة.

---

## 22. المعلومات المطلوبة لاحقاً قبل كتابة الكود (Required Code Inputs)

### 🔴 الأولوية القصوى (Priority 1) — حاسمة لبدء الاتصال:
1. تعريف `enum MessageType` من الكود الأصلي لمعرفة الأرقام الفعلية.
2. كود دالتي `WriteMessage` و `ReadMessage` في `ChatProtocol.cs` للتأكد من أحجام وترتيب البايتات.
3. أرقام المنافذ الافتراضية المحددة في `ServerForm.cs` أو `appsettings.json`.
4. نموذج `AuthPayload` لمعرفة أسماء الحقول المطلوبة لتسجيل الدخول.

### 🟡 الأولوية المتوسطة (Priority 2) — حاسمة للمحادثات والملفات:
1. كود إرسال واستقبال الرسائل النصية في الخادم لمعرفة مسار الـ Broadcast وهل يتم عبر TCP أم SignalR.
2. كود إرسال واستقبال الملفات في الخادم لمعرفة الهيكل الدقيق لحزمة الملف.
3. نموذج `CollaborationSnapshot` لمعرفة تركيبة البيانات الأولية التي يرسلها الخادم.

### 🟢 الأولوية الثالثة (Priority 3) — للميزات التكميلية:
1. مسارات ودوال `CollaborationHub.cs`.
2. بنية أوامر `CollaborationCommandType` (مثل Typing و Presence).
3. كود `CallHub.cs` لتأكيد صيغة رسائل الإشارات إن رغبنا في عرضها كإشعارات فقط.

---

## 23. خطة التنفيذ المستقبلية المنضبطة (Disciplined Execution Roadmap)

* **المرحلة الأولى: فحص المعطيات وتأكيد البروتوكول (عند توفر الأكواد المفتاحية):**
  - استخراج الأرقام الحقيقية للـ Enums وحجم الرأس وشكل الـ Payloads.
* **المرحلة الثانية: بناء الواجهات المستقلة (UI Mockups & Design):**
  - بناء شاشة اتصال مرنة تقبل IP والمنفذ واسم المستخدم.
  - بناء شاشة مساحة العمل (تبويب الشات، تبويب الملفات، تبويب الأعضاء) بمكونات Material 3 دون ربط شبكي مسبق.
* **المرحلة الثالثة: طبقة المقابس الشبكية المعزولة (TCP Socket Manager):**
  - برمجة عميل المقبس على `Dispatchers.IO` مع إدارة حالات الاتصال والقطع.
* **المرحلة الرابعة: مواءمة بروتوكول ChatProtocol بدقة البايتات:**
  - برمجة فك وتركيب الحزم وفق ما يتم إثباته من ملف `ChatProtocol.cs`.
* **المرحلة الخامسة: اختبار المصادقة والمزامنة الحية:**
  - تجربة إرسال الدخول واستقبال الرد وقائمة المستخدمين في معمل حقيقي.
* **المرحلة السادسة: تفعيل المحادثات وتبادل الملفات:**
  - إرسال واستقبال الرسائل والملفات خطوة بخطوة مع الخادم.

---

## 24. دليل المناقشة الأكاديمية الصادقة أمام الدكتور (Academic Defense Guide)

عند مناقشة المشروع أمام اللجنة أو أستاذ المادة، احرص على الصياغة العلمية الصادقة والابتعاد التام عن المبالغات التسويقية:

1. **حول معمارية الاتصال:**
   - *"قمنا بتصميم عميل أندرويد ليتصل مباشرة بخادم C# عبر مقابس TCP في الشبكة المحلية دون الاعتماد على خدمات سحابية وسيطة، وذلك للحفاظ على خصوصية بيانات المعمل وسرعة النقل المحلي."*
2. **حول مطابقة البروتوكول الثنائي (Binary Protocol Alignment):**
   - *"التحدي الأساسي في الربط بين .NET و Android يكمن في طبقة البايتات الثنائية؛ حيث تمت مراعاة ترتيب البايتات (Endianness) وتجزئة حزم TCP عبر حلقة تجميع معتمدة على طول الحمولة المحدد في الرأس."*
3. **حول الأمانة الأكاديمية للميزات غير المكتملة:**
   - *"يوضح التوثيق الهندسي للنظام أن تسجيل الفيديو هو معالجة وسائط محلية بواسطة كاميرا الجهاز، وأن مكالمات الفيديو الحية غير مطبقة لأن الخادم لا يحتوي على خادم وسائط (Media Server) أو WebRTC حقيقي، واقتصرت فئة CallHub على بنية إشارات أولية فقط."*
4. **حول أداء واجهات المستخدم:**
   - *"تم عزل عمليات الإدخال والإخراج وقراءة المقابس بالكامل داخل خيوط خلفية عبر Kotlin Coroutines على خيط `Dispatchers.IO`، مع استخدام StateFlow لتحديث واجهات Jetpack Compose بشكل غير تزامني يمنع تجمد التطبيق."*

---

## 25. الخلاصة والقرار النهائي (Executive Decision & Conclusion)

فيما يلي الإجابات الصريحة والنهائية على الأسئلة الجوهرية قبل اتخاذ أي خطوة برمجية:

| # | السؤال الهندسي | الإجابة الصريحة والنهائية |
| :---: | :--- | :--- |
| **1** | **هل نستطيع بدء تصميم واجهات Android؟** | **نعم، بأمان كامل.** يمكن تصميم واجهات Compose (شاشة الاتصال، مساحة العمل، قائمة الشات) كواجهات مرئية مستقلة تماماً عن منطق الشبكة. |
| **2** | **هل نستطيع كتابة كود الاتصال النهائي الآن؟** | **لا.** كتابة كود اتصال نهائي قبل معرفة تفاصيل البايتات وقيم الأوامر ستكون مجرد تخمين معرض للفشل بنسبة 100%. |
| **3** | **هل نعرف المنافذ الفعلية؟** | **لا.** المنافذ (مثل 5000) غير مؤكدة؛ ولذلك صممت واجهة أندرويد لتكون ديناميكية تتيح للمستخدم إدخال المنفذ يدوياً. |
| **4** | **هل نعرف بنية ChatProtocol بشكل مؤكد؟** | **لا.** نعرف النمط العام فقط، لكن تحديد أحجام البايتات وطريقة كتابتها في C# يحتاج مراجعة مباشرة لـ `ChatProtocol.cs`. |
| **5** | **هل نعرف قيم MessageType؟** | **لا.** الأرقام (مثل 1, 2, 3...) مجهولة ويحظر اختراعها. |
| **6** | **هل نعرف شكل AuthPayload الحقيقي؟** | **نعرف المفهوم العام فقط** (اسم مستخدم)، لكن أسماء الحقول الدقيقة في JSON غير مثبتة. |
| **7** | **هل نعرف طريقة نقل الملفات الحقيقية؟** | **لا.** غير محسوم هل ترسل كـ Stream متتابع، أم حزم Chunked، أم Base64. |
| **8** | **هل نعرف مسار SignalR؟** | **لا.** المسار مجهول بدقة ويحتاج فحص ملف `Program.cs`. |
| **9** | **هل توجد مكالمات فيديو حقيقية؟** | **لا توجد إطلاقاً** مكالمات فيديو حية؛ والموجود هو إشارات أولية فقط. |
| **10** | **هل يوجد OCR؟** | **لا يوجد إطلاقاً.** |
| **11** | **ما الذي يمكن تنفيذه بأمان الآن؟** | تصميم واجهات المستخدم (UI Layouts)، تهيئة معمارية MVVM، وتجهيز هياكل الكود الأساسية مع جعل معلمات الاتصال متغيرة وديناميكية. |
| **12** | **ما الذي يجب تأجيله حتى يتم فحص الكود؟** | ربط المقبس الفعلي، تشفير حزم البايتات، وتثبيت قيم الـ Enums. |

---
**نهاية الوثيقة المراجعة والمدققة.**  
*هذه النسخة تمثل التقييم العلمي والأكاديمي النزيه وتلغي أي ادعاءات قطعية غير مدعومة بالدليل في التقارير السابقة.*
