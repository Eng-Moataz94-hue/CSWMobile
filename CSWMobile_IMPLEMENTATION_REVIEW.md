# تقرير المراجعة الهندسية والتدقيق الجنائي لكود تطبيق CSWMobile المنجز
**اسم الملف المرجعي:** `CSWMobile_IMPLEMENTATION_REVIEW.md`  
**تاريخ المراجعة:** 2026-09-05  
**طبيعة الوثيقة:** مراجعة هندسية جنائية وتدقيق صارم لكود Kotlin وJetpack Compose وملفات Gradle والـ Manifest التي تم إنشاؤها وتجميعها في المشروع، لبيان الفجوة الدقيقة بين نجاح التجميع (Compilation) والتوافق الفعلي مع خادم `ChatServer` (C#).

---

## الحكم الهندسي النهائي القطعي على حالة التطبيق الحالية:
> ⚖️ **الحكم المعتمد بناءً على الأدلة والتحليل الفعلي للكود:**  
> **[ TCP Client نظري غير مختبر تكاملياً — Architectural Prototype ]**  
> 
> * **التطبيق ليس متوافقاً بعد مع الخادم الحقيقي**، ولا يصح وصفه بأنه «متوافق 100%».
> * التطبيق نجح برمجياً في اجتياز مرحلة التجميع وبناء الـ APK (`Build succeeded`) ككود Android Native سليم الصياغة، لكنه **مبني على افتراضات بروتوكولية غير مثبتة قطعياً من كود الخادم الأصلي**.
> * لم يتم إجراء أي اختبار اتصال حقيقي أو تكامل شبكي حي (End-to-End Integration Test) يثبت صحة تبادل البايتات مع الخادم.

---

## 1. تصحيح حالة المشروع والاعتراف بما تم إنشاؤه

نقرّ بصراحة ووضوح أن المشروع قد انتقل فعلياً من مجرد قالب أولي إلى كود تنفيذي متكامل الأركان داخل بيئة Android، حيث تم إنشاء وتعديل 15 ملفاً رئيساً كالتالي:

1. **تم إنشاء مشروع Android Native كامل:** بنظام Gradle الحديث (AGP 8.9.1، Kotlin 2.0.21، Compose BOM 2025.02.00).
2. **تمت كتابة 8 ملفات Kotlin برمجية أساسية:**
   - `MessageType.kt`
   - `CollaborationModels.kt`
   - `ChatProtocol.kt`
   - `TcpSocketManager.kt`
   - `CSWViewModel.kt`
   - `ConnectionScreen.kt`
   - `WorkspaceScreen.kt`
   - `MainActivity.kt`
3. **تم تعديل ملفات البناء والإعدادات والترخيص:**
   - `AndroidManifest.xml` (إضافة أذونات `INTERNET`, `ACCESS_NETWORK_STATE`, و `usesCleartextTraffic="true"`).
   - `app/build.gradle.kts` (تخصيص `applicationId`، وتفعيل مكتبة Coil للصور).
   - `settings.gradle.kts` و `metadata.json` و `strings.xml` (تسمية التطبيق CSWMobile).
   - `ic_launcher_background.xml` و `ic_launcher_foreground.xml` (تصميم أيقونة مخصصة).
4. **تم تجميع التطبيق بنجاح كامل:** أداة `compile_applet` أعادت `Build succeeded - the applet is compiled`.
5. **ما لم يتم إثباته قط:**
   - لم يثبت أن التطبيق يستطيع فتح جلسة مقبس حية مع `ChatServer.cs`.
   - لم يثبت أن الخادم يقبل قيم `MessageType` أو يتعرف عليها.
   - لم يثبت نجاح تسجيل الدخول والمصادقة `Auth`.
   - لم يثبت استقبال كائن `CollaborationSnapshot` دون انهيار في تحليل JSON.
   - لم يثبت تبادل رسائل الدردشة الحية.
   - لم يثبت إرسال أو استقبال أي ملفات أو صور.
   - لم يثبت عمل SignalR (لأنه غير مبرمج في تطبيق أندرويد حالياً).
   - لم يثبت التكامل التبادلي مع عميل Windows Client (`ChatClient`).

---

## 2. الفرق الهندسي بين مستويات النجاح التسعة (لماذا لا يُعد البناء دليلاً على التوافق؟)

| المستوى | الحالة في المشروع | التفسير العلمي والتقني |
| :--- | :---: | :--- |
| **1. Compilation Success** | **ناجح (متحقق)** | يعني فقط خلو كود Kotlin من الأخطاء النحوية (Syntax) وصحة استيراد المكتبات (Imports) ومطابقة الأنواع في بيئة البناء. |
| **2. APK Generation** | **ناجح (متحقق)** | تحويل كود Kotlin إلى بايتات DEX وتوقيع الحزمة لإنتاج ملف قابل للتثبيت. |
| **3. App Launch Success** | **ناجح (متحقق)** | عمل واجهة Compose وبدء الشاشة الأولى دون انهيار عند التشغيل. |
| **4. TCP Connection Success** | **غير مثبت** | قدرة الهاتف على إتمام مصافحة TCP الثلاثية (SYN, SYN-ACK, ACK) مع IP الخادم على الشبكة المحلية دون حظر الجدار الناري. |
| **5. Authentication Success** | **غير مثبت** | قراءة الخادم لحزمة الدخول ومطابقة حقولها وقبول اسم المستخدم دون رمي استثناء وفصل المقبس. |
| **6. Snapshot Reception Success**| **غير مثبت** | استقبال العميل لحزمة الحالة الأولية وتحليل نصوص JSON بدقة دون خطأ في أسماء الحقول أو أنواعها. |
| **7. Message Exchange Success** | **غير مثبت** | إرسال رسالة شات واستقبالها من قِبل عميل Windows عبر خادم C#. |
| **8. File Transfer Success** | **غير مثبت** | ضخ بايتات الملف واستقبالها وحفظها على القرص دون تلف في المحتوى. |
| **9. Full Integration Success** | **غير مثبت إطلاقاً** | عمل النظامين كمنظومة عمل موحدة ومستقرة في كافة سيناريوهات الاستخدام وإعادة الاتصال. |

> **الصياغة العلمية الدقيقة:**  
> **«تم تجميع تطبيق Android بنجاح تام، لكن التوافق الفعلي مع الخادم لم يُثبت بعد باختبار تكامل كامل.»**

---

## 3. المراجعة الفنية التفصيلية لكل ملف تم إنشاؤه في المشروع

---

### 1) ملف `MessageType.kt`
* **المسار:** `/app/src/main/java/com/example/csw/model/MessageType.kt`
* **وظيفته:** تعريف تعداد الرموز العددية لأنواع الرسائل.
* **الدوال الموجودة:** `fromCode(code: Int): MessageType`.
* **من يستدعيه:** `ChatProtocol.kt`, `TcpSocketManager.kt`, `CSWViewModel.kt`.
* **الملفات التي يستدعيها:** لا يستدعي ملفات خارجية.
* **البيانات الداخلة:** كود عددي (Int).
* **البيانات الخارجة:** عنصر Enum.
* **هل يعتمد على كود C# مؤكد؟** **لا إطلاقاً.**
* **الافتراضات والقيم المخترعة:** الأرقام (`1` للـ Auth، `4` للشات، `8` للـ Snapshot) هي **تخمينات مستنتجة وليست مؤكدة من الكود الأصلي للخادم**.
* **هل يحتاج لتعديل بعد فحص الخادم؟** **نعم وبشكل مؤكد؛** يجب تعديل الأرقام فور مطابقتها مع ملف enum الحقيقي في C#.

---

### 2) ملف `CollaborationModels.kt`
* **المسار:** `/app/src/main/java/com/example/csw/model/CollaborationModels.kt`
* **وظيفته:** تعريف نماذج البيانات والـ DTOs مع دوال تحويل الـ JSON.
* **الدوال الموجودة:** `toJson()`, `fromJson()` لكافة النماذج (`AuthPayload`, `SendMessagePayload`, `CollaborationSnapshot`, `ConnectedUser`, `FileItem`, `FileMetadataPayload`).
* **من يستدعيه:** `CSWViewModel.kt`, `WorkspaceScreen.kt`.
* **الملفات التي يستدعيها:** مكتبات `org.json.JSONObject` و `org.json.JSONArray`.
* **البيانات الداخلة والخارجة:** سلاسل JSON وكائنات Data Classes.
* **هل يعتمد على كود C# مؤكد؟** يعتمد على المفاهيم العامة فقط (اسم المستخدم، نص الرسالة، اسم الملف).
* **الافتراضات والقيم المخترعة:** أسماء الحقول مثل `DeviceName`, `ClientType`, `IsImage`, `ServerTime` هي افتراضات تحسينية أضيفت لتقليل فرص الخطأ لكنها قد تكون غير موجودة أو مختلفة في الخادم الأصلي.
* **هل يحتاج لتعديل بعد فحص الخادم؟** **نعم،** لتثبيت أسماء الحقول بدقة وحذف أو تعديل أي حقل لا يدعمه الخادم.

---

### 3) ملف `ChatProtocol.kt`
* **المسار:** `/app/src/main/java/com/example/csw/protocol/ChatProtocol.kt`
* **وظيفته:** تشفير وفك تشفير بايتات الحزم عبر مقبس TCP.
* **الدوال الموجودة:** `readPacket()`, `writePacket()`, `readFully()`.
* **من يستدعيه:** `TcpSocketManager.kt`.
* **الملفات التي يستدعيها:** `MessageType.kt`, `ByteBuffer`, `ByteOrder`.
* **البيانات الداخلة:** تدفق بايتات `InputStream` أو نصوص وحزم للإرسال على `OutputStream`.
* **البيانات الخارجة:** كائن `RawPacket`.
* **هل يعتمد على كود C# مؤكد؟** **افتراض هندسي غير مؤكد.** افترضنا أن الهيدر هو 8 بايت (4 نوع + 4 طول) بنظام Little-Endian. إذا كان الخادم يكتب `BinaryWriter.Write(string)` أو يكتب نوع الرسالة كـ `byte` واحد، فسيفشل هذا الكود فوراً.
* **هل يحتاج لتعديل بعد فحص الخادم؟** **نعم، بنسبة ترجيح عالية جداً** بمجرد قراءة كود `ChatProtocol.cs` في دوت نت.

---

### 4) ملف `TcpSocketManager.kt`
* **المسار:** `/app/src/main/java/com/example/csw/network/TcpSocketManager.kt`
* **وظيفته:** إدارة دورة حياة اتصال المقبس الشبكي (فتح، إغلاق، قراءة خلفية، إرسال متزامن).
* **الدوال الموجودة:** `connect()`, `startReaderLoop()`, `sendPacket()`, `sendRawPacket()`, `disconnect()`.
* **من يستدعيه:** `CSWViewModel.kt`.
* **الملفات التي يستدعيها:** `ChatProtocol.kt`, Kotlin Coroutines, `java.net.Socket`.
* **البيانات الداخلة:** عنوان IP ورقم المنفذ، وحزم البيانات.
* **البيانات الخارجة:** تدفقات Flow لحالات الاتصال والحزم المستقبلة.
* **هل يعتمد على كود C# مؤكد؟** يعتمد على معايير شبكات TCP القياسية المستقلة عن لغة الخادم.
* **الافتراضات:** يفترض أن الخادم يستقبل الاتصال ويبقى المقبس مفتوحاً كـ Persistent Connection دون إنهاء مفاجئ.
* **هل يحتاج لتعديل بعد فحص الخادم؟** الكود الهيكلي ممتاز، لكن قد يحتاج لتعديل آليات المصافحة ومؤقتات انتهاء الاتصال (Keep-Alive/Ping).

---

### 5) ملف `CSWViewModel.kt`
* **المسار:** `/app/src/main/java/com/example/csw/viewmodel/CSWViewModel.kt`
* **وظيفته:** وسيط إدارة الحالة (MVVM ViewModel)، وتوجيه الحزم وعمليات الإرسال.
* **الدوال الموجودة:** `connect()`, `performAuthHandshake()`, `handleIncomingPacket()`, `sendChatMessage()`, `onUserTyping()`, `sendFile()`, `disconnect()`.
* **من يستدعيه:** `MainActivity.kt`.
* **الملفات التي يستدعيها:** `TcpSocketManager`, `CollaborationModels`, `MessageType`.
* **البيانات الداخلة والخارجة:** أحداث وتفاعلات الواجهة وحزم الشبكة، وتوليد كائن `WorkspaceUiState`.
* **هل يعتمد على كود C# مؤكد؟** **مبني على تسلسل افتراضي:** إرسال Auth ثم انتظار AuthSuccess أو Snapshot.
* **هل يحتاج لتعديل بعد فحص الخادم؟** **نعم؛** ليطابق بدقة دورة الرسائل الحقيقية في الخادم.

---

### 6) ملف `ConnectionScreen.kt`
* **المسار:** `/app/src/main/java/com/example/csw/ui/screens/ConnectionScreen.kt`
* **وظيفته:** واجهة إدخال بيانات الاتصال (IP, Port, Username, Password).
* **الدوال الموجودة:** `ConnectionScreen()` (Composable Function).
* **من يستدعيه:** `MainActivity.kt`.
* **الملفات التي يستدعيها:** مكونات Jetpack Compose المادية (M3).
* **تقييم الاعتمادية:** واجهة مستخدم مستقلة تماماً، مجهزة بنجاح، وتوفر حقولاً ديناميكية تمنع الانهيار إن اختلف المنفذ أو الـ IP.

---

### 7) ملف `WorkspaceScreen.kt`
* **المسار:** `/app/src/main/java/com/example/csw/ui/screens/WorkspaceScreen.kt`
* **وظيفته:** واجهة مساحة العمل التعاونية الموحدة (شات، ملفات، أعضاء، دليل أكاديمي).
* **الدوال الموجودة:** `WorkspaceScreen`, `ChatView`, `FilesView`, `MembersView`, `AcademicInfoView`, `MessageBubble`.
* **من يستدعيه:** `MainActivity.kt`.
* **الملفات التي يستدعيها:** Compose Components, Coil `AsyncImage`.
* **تقييم الاعتمادية:** واجهة عرضية كاملة، لكن بعض الوظائف (مثل تنزيل الملفات من القائمة) مجرد أزرار عرضية لأن الخادم لم يثبت بعد بروتوكول طلب استرجاع الملفات.

---

### 8) ملف `MainActivity.kt`
* **المسار:** `/app/src/main/java/com/example/MainActivity.kt`
* **وظيفته:** نقطة انطلاق التطبيق والتبديل الحركي (Crossfade) بين شاشة الاتصال ومساحة العمل بحسب حالة الجلسة.
* **الدوال الموجودة:** `onCreate()`.
* **من يستدعيه:** نظام تشغيل Android عند النقر على أيقونة التطبيق.
* **تقييم الاعتمادية:** كود نظيف ومتوافق مع أحدث ممارسات Android Compose الحديثة.

---

### 9) ملف `AndroidManifest.xml`
* **المسار:** `/app/src/main/AndroidManifest.xml`
* **وظيفته:** تعريف هوية التطبيق وتصاريح الشبكة.
* **التعديل المنجز:** إضافة `INTERNET`, `ACCESS_NETWORK_STATE`, و `usesCleartextTraffic="true"` (للسماح بالاتصال بمقابس الشبكة المحلية غير المشفرة).
* **تقييم الاعتمادية:** مؤكد هندسياً وإلزامي لعمل أي اتصال محلي.

---

### 10) ملفات البناء `build.gradle.kts` و `settings.gradle.kts`
* **المسار:** `/app/build.gradle.kts` و `/settings.gradle.kts`
* **وظيفته:** ضبط الاعتماديات وتحديد `applicationId` الفريد وتسمية المشروع.
* **التعديل المنجز:** تفعيل مكتبة Coil للصور وإلغاء تبعات التعارض.
* **تقييم الاعتمادية:** مؤكد وناجح في التجميع.

---

## 4. المراجعة الجنائية الصارمة لقيم `MessageType.kt`

تم فحص القيم البرمجية المنفذة في كود Kotlin ومقارنتها بما هو مثبت من كود الخادم C#:

| القيمة البرمجية في Kotlin | الاسم في Android | الدليل المتاح من كود C# | هل هي مؤكدة أم افتراض؟ | التقييم الهندسي والقرار |
| :---: | :--- | :--- | :---: | :--- |
| **0** | `UNKNOWN` | لا يوجد دليل | **افتراض وقائي** | وضع لحماية التطبيق من الانهيار عند استقبال بايتات غير معروفة. |
| **1** | `AUTH_REQUEST` | تقرير سابق فقط | **افتراض غير مؤكد** | **لا يجوز الاعتماد عليها** في التكامل حتى مراجعة enum الحقيقي في C#. |
| **2** | `AUTH_SUCCESS` | تقرير سابق فقط | **افتراض غير مؤكد** | لا يوجد دليل صريح على الرقم 2. |
| **3** | `AUTH_FAILED` | تقرير سابق فقط | **افتراض غير مؤكد** | لا يوجد دليل صريح على الرقم 3. |
| **4** | `CHAT_MESSAGE` | تقرير سابق فقط | **افتراض غير مؤكد** | لا يوجد دليل صريح على الرقم 4. |
| **5** | `SEND_FILE` | تقرير سابق فقط | **افتراض غير مؤكد** | لا يوجد دليل صريح على الرقم 5. |
| **6** | `FILE_DATA_CHUNK` | تقرير سابق فقط | **افتراض غير مؤكد** | لا يوجد دليل صريح على الرقم 6. |
| **7** | `FILE_ACK` | تقرير سابق فقط | **افتراض غير مؤكد** | لا يوجد دليل صريح على الرقم 7. |
| **8** | `COLLABORATION_SNAPSHOT` | تقرير سابق فقط | **افتراض غير مؤكد** | لا يوجد دليل صريح على الرقم 8. |
| **9** | `COMMAND` | تقرير سابق فقط | **افتراض غير مؤكد** | لا يوجد دليل صريح على الرقم 9. |
| **10** | `PRESENCE_UPDATE` | تقرير سابق فقط | **افتراض غير مؤكد** | لا يوجد دليل صريح على الرقم 10. |
| **11** | `TYPING_INDICATOR` | تقرير سابق فقط | **افتراض غير مؤكد** | لا يوجد دليل صريح على الرقم 11. |
| **12** | `DISCONNECT` | تقرير سابق فقط | **افتراض غير مؤكد** | لا يوجد دليل صريح على الرقم 12. |
| **13** | `HEARTBEAT` | تقرير سابق فقط | **افتراض غير مؤكد** | لا يوجد دليل صريح على الرقم 13. |

> **القرار:**  
> **جميع القيم العددية المذكورة أعلاه غير مؤكدة من الكود المصدري الأصلي للخادم، ولا يجوز افتراض تطابقها مع الخادم دون فحص ملف C# الفعلي.**

---

## 5. التدقيق الفني المباشر لتنفيذ `ChatProtocol.kt`

تمت مراجعة أسطر ملف `ChatProtocol.kt` للتأكد من سلوكها التقني ومعالجة الحالات الحدية:

1. **حجم الهيدر وترتيب الحقول:**  
   الكود يقرأ صراحة 8 بايتات عبر مصفوفة `ByteArray(8)`.  
   - 4 بايتات أولى: تستخرج عبر `headerBb.int` وتعين كـ `typeCode`.
   - 4 بايتات ثانية: تستخرج عبر `headerBb.int` وتعين كـ `payloadLength`.
   - التقييم: إذا كان الخادم يرسل `typeCode` كـ 2 بايت (Short) أو بايت واحد (Byte)، فستتم قراءة طول الحمولة بشكل خاطئ تماماً ويفشل الاتصال.
2. **الـ Endianness:**  
   الكود يفرض صراحة `order(ByteOrder.LITTLE_ENDIAN)`. هذا يطابق معيار معالجات Intel/AMD ومعيار `BinaryWriter` القياسي في دوت نت عند كتابة أرقام صحيحة، لكنه غير مؤكد إن كان كود C# الأصلي يعتمد ذلك تحديداً.
3. **سلامة التجميع ومنع الانهيارات (readFully & OOM Protection):**  
   - الكود يحتوي على فحص لمنع الانهيار: `if (payloadLength < 0 || payloadLength > MAX_PAYLOAD_SIZE) throw IllegalArgumentException(...)`.
   - الكود يطبق دالة `readFully` بحلقة `while (bytesReadTotal < length)` تضمن قراءة كامل الحزمة وتمنع تجزئة حزم الـ Wi-Fi.
   - الكود يتعامل مع نهاية التدفق `EOFException` إذا أرجع المقبس `-1`.
4. **طريقة نقل الملفات (الخلل الافتراضي الكبير):**  
   - في الدالة الحالية، يتم إرسال ملف الميتاداتا كحزمة JSON مستقلة ثم يتم استدعاء `sendRawPacket()` لبايتات الملف.
   - **هذه الطريقة غير مثبتة من الخادم:** هل الخادم يتوقع أن يكون رأس الملف محتوياً على اسم الملف وطوله في نفس الحزمة؟ هل يتوقع إرسال بايتات الملف مجزأة؟ الكود الحالي يفترض ذلك دون سند من كود الخادم.

---

## 6. التدقيق الفني لـ `TcpSocketManager.kt`

1. **طبيعة المقبس:** يستخدم مقبس TCP حقيقي أصلي من مكتبة `java.net.Socket`.
2. **عزل الخيوط:** جميع العمليات مقيدة بـ `withContext(Dispatchers.IO)` وحلقة القراءة تعمل في `coroutineScope.launch(Dispatchers.IO)`، مما يضمن استحالة حدوث استثناء `NetworkOnMainThreadException`.
3. **مهلة الاتصال:** محددة بـ 6000 ميلي ثانية (`newSocket.connect(address, timeoutMs)`).
4. **الحماية من التضارب (Thread Safety & Race Conditions):**  
   - يستخدم كائن `Mutex` حصري لحماية عمليات الإرسال (`sendMutex.withLock`) مما يمنع تداخل بايتات رسالتين إن تم إرسالهما في نفس اللحظة من واجهتين مختلفتين.
5. **حلقة القراءة (Reader Loop):**  
   - يتم إلغاء أي وظيفة قارئ سابقة (`readerJob?.cancel()`) قبل بدء قارئ جديد، مما يمنع وجود حلقتي قراءة متنافستين على نفس المقبس.
6. **إغلاق الموارد (Resource Cleanup):**  
   - دالة `disconnect()` تغلق `InputStream` و `OutputStream` ومقبس `Socket` وتلغي مهمة الـ Coroutine في كتلة `finally`.
7. **التعامل مع الاستثناءات:**  
   - يتعامل بدقة مع `ECONNREFUSED` (الخادم غير مشغل أو مغلق)، `ETIMEDOUT` (الجدار الناري يحظر المنفذ)، و `EHOSTUNREACH` (الـ IP غير موجود على الشبكة).

---

## 7. فحص ومطابقة النماذج في `CollaborationModels.kt`

| النموذج في Kotlin | الحقول المنفذة في Android | الحقول المتوقعة في C# | نوع البيانات | هل التطابق مؤكد؟ | الفروقات المحتملة |
| :--- | :--- | :--- | :---: | :---: | :--- |
| **`AuthPayload`** | `username`, `password`, `clientType`, `deviceName` | `Username`, `Password` | String | **غير مؤكد** | قد لا يحتوي خادم C# على حقلي `clientType` أو `deviceName`. تم دعم PascalCase و camelCase برمجياً لكن الحقول الزائدة قد تسبب رفضاً إن كان الـ Deserializer في C# صارماً. |
| **`SendMessagePayload`**| `messageId`, `senderUsername`, `content`, `timestamp`, `messageType` | `SenderUsername`, `Content` | String | **غير مؤكد** | معرف الرسالة `messageId` قد يكون GUID في C# بدلاً من String عادي، وحقل `timestamp` قد يكون كائن `DateTime` في C# بدلاً من نص بتنسيق "HH:mm". |
| **`CollaborationSnapshot`**| `connectedUsers`, `recentMessages`, `sharedFiles`, `serverTime` | `ConnectedUsers`, `Messages` | Arrays / Objects | **غير مؤكد** | بنية القوائم في خادم C# قد تختلف تسميتها (مثلاً `Users` بدلاً من `ConnectedUsers`، أو `History` بدلاً من `RecentMessages`). |
| **`ConnectedUser`** | `username`, `isOnline`, `status`, `clientType`, `ipAddress` | `Username`, `IsOnline` | String / Bool | **غير مؤكد** | حقلا `status` و `clientType` قد لا يكونان معرفين في كائن المستخدم في الخادم. |
| **`FileItem`** | `id`, `fileName`, `fileSize`, `sender`, `timestamp`, `isImage` | `FileName`, `FileSize` | String / Long | **غير مؤكد** | حجم الملف في C# قد يكون `long` أو `int`، ومعرف الملف قد لا يكون موجوداً في الخادم كـ GUID. |

---

## 8. تدقيق منطق `CSWViewModel.kt`

1. **دورة المصادقة (`performAuthHandshake`):**  
   الكود يرسل `AuthPayload` فور تحول حالة المقبس إلى `Connected`. هذا افتراض هندسي منطقي، لكنه قد يفشل إذا كان الخادم يتوقع أن يرسل هو ترحيباً أولاً (Server Banner/Greeting)، أو إذا كان الخادم يتوقع تلقي اسم المستخدم كنص مجرد دون تغليف في JSON.
2. **الانتقال التلقائي للواجهة:**  
   الـ ViewModel يقوم بنقل الشاشة من `ConnectionScreen` إلى `WorkspaceScreen` فور استلام رسالة من نوع `AUTH_SUCCESS` أو `COLLABORATION_SNAPSHOT`. إذا أرسل الخادم رسالة بنوع غير مسجل أو لم يرسل رسالة نجاح صريحة، سيظل التطبيق عالقاً في شاشة الاتصال.
3. **إرسال الرسائل النصية:**  
   يتم تحديث الواجهة محلياً فور الإرسال (Optimistic UI Update) ثم ضخ الحزمة. إذا فشل المقبس لاحقاً يتم إشعار المستخدم عبر شريط الخطأ.
4. **رفع الملفات:**  
   الكود يعتمد على `contentResolver` لقراءة الملف محلياً واستخراج اسمه وحجمه، ثم يرسل حزمة `SEND_FILE` متبوعة بالبايتات الخام. هذا الإجراء غير مثبت من جانب بروتوكول الخادم.

---

## 9. المراجعة التشغيلية لواجهات Compose (ما يعمل وما لا يعمل)

### أ) شاشة الاتصال (`ConnectionScreen.kt`):
* **ما يعمل فعلياً:**
  - حقول إدخال النصوص (IP، المنفذ، اسم المستخدم، كلمة المرور).
  - منع استخدام `localhost` أو `127.0.0.1` وعرض لافتة توجيهية للمستخدم لإدخال IP شبكة الـ Wi-Fi.
  - مؤشر التحميل الدوار عند النقر على "الاتصال".
  - استقبال رسائل الخطأ وعرضها في بطاقة منبثقة مع زر الإلغاء Dismiss.
* **ما يحتاج لخادم حقيقي:** نجاح الاتصال الفعلي وإتمام المصادقة.

### ب) شاشة مساحة العمل (`WorkspaceScreen.kt`):
* **ما يعمل فعلياً (UI Logic):**
  - شريط العنوان العلوي ويعرض اسم المستخدم وعنوان الـ IP والمنفذ وزر قطع الاتصال.
  - شريط التنقل السفلي والتبديل السلس بين التبويبات الأربعة (Chat, Files, Members, Defense Info).
  - حقل إدخال الرسائل وزر الإرسال وزر المرفقات واستدعاء منتقي ملفات النظام (File Picker).
  - فقاعات الرسائل وتمييز رسائل المستخدم الحالي عن رسائل الزملاء.
  - تبويب المعلومات الأكاديمية (Defense Info) ويعرض شروحاً نصية ثابتة لمساعدة الطالب في المناقشة.
* **ما هو مجرد واجهة عرض (Placeholder / Unconnected Action):**
  - **تنزيل الملفات:** لا يوجد كود يطلب تنزيل الملف من الخادم عند الضغط عليه في قائمة الملفات؛ لعدم توفر بروتوكول طلب الملف في الخادم.
  - **مؤشر الكتابة Typing:** يرسل حدثاً تجريبياً، لكنه لن يظهر إلا إذا كان الخادم يدعم إعادة بث حزمة الـ Typing.
  - **مكالمات الصوت والفيديو:** غير مبرمجة لعدم وجودها في الخادم.

---

## 10. مراجعة المنافذ وإعدادات الشبكة

1. **المنفذ الافتراضي 5000:**  
   تم وضعه كقيمة افتراضية في حقل الإدخال لتسهيل التجربة، **لكنه ليس قيمة قطعية مثبتة من الكود المصدري**.
2. **شأن SignalR:**  
   التطبيق الحالي **لا يحتوي على أي اتصال SignalR** ولا يستدعي أي Hub. الاتصال الحالي محصور بنسبة 100% في مقبس TCP المباشر.
3. **عدد المنافذ المطلوبة:**  
   يعتمد التطبيق الحالي على **منفذ واحد فقط** (منفذ الـ TCP).

---

## 11. مراجعة الأمان وتصاريح النظام

1. **تصريح `INTERNET` و `ACCESS_NETWORK_STATE`:** مفعلان بشكل صحيح في `AndroidManifest.xml`.
2. **خاصية `android:usesCleartextTraffic="true"`:**  
   مفعلة في ملف المانيفست، وهو إجراء إلزامي، لأن نظام أندرويد منذ الإصدار 9 (API 28) يمنع افتراضياً أي اتصالات شبكية غير مشفرة (Cleartext / HTTP / Unencrypted TCP). تفعيل هذه الخاصية يسمح بالاتصال بمقابس حواسيب المعمل المحلية التي لا تستخدم شهادات TLS/SSL.
3. **أمان الذاكرة والـ Buffer:** تم وضع حد أقصى للحمولة (50 ميجابايت) في `ChatProtocol.kt` لمنع هجمات حجب الخدمة أو أخطاء نفاد الذاكرة (`OutOfMemoryError`).

---

## 12. قائمة الأخطاء والافتراضات المكتشفة في الكود المنجز

1. **الخطأ الافتراضي الأول (قيم الـ Enums):**  
   افتراض أن `MessageType.AUTH_REQUEST = 1` و `CHAT_MESSAGE = 4`. لا يوجد دليل من الخادم يثبت ذلك.
2. **الخطأ الافتراضي الثاني (طول ونوع الهيدر):**  
   افتراض أن الهيدر حتماً 8 بايتات بصيغة Little-Endian.
3. **الخطأ الافتراضي الثالث (بروتوكول إرسال الملفات):**  
   افتراض أن إرسال الملف يتم عبر إرسال ميتاداتا JSON ثم ضخ بايتات الملف الخام بشكل منفصل عبر `sendRawPacket`.
4. **الخطأ الافتراضي الرابع (حقول النماذج في JSON):**  
   افتراض احتواء الـ JSON على حقول مثل `ClientType` و `DeviceName` و `IsImage`.

---

## 13. جدول الحقائق المؤكدة مقابل المعلومات غير المؤكدة

| الموضوع | الحالة المؤكدة هندسياً | الافتراض غير المؤكد برمجياً |
| :--- | :--- | :--- |
| **بيئة البناء والتطبيق** | تم بناء وتجميع تطبيق Android بنجاح تام (`Build succeeded`). | أن التطبيق متوافق فعلياً مع الخادم. |
| **هيكل الواجهات** | واجهات Jetpack Compose جاهزة وتعمل وتستجيب للتنقل والمدخلات. | أن البيانات المعروضة تم سحبها من قاعدة بيانات الخادم. |
| **محرك المقابس** | محرك `TcpSocketManager` يعمل بنجاح على `Dispatchers.IO` وبحماية Mutex. | أن الخادم يوافق على آلية المصافحة المطبقة. |
| **البروتوكول** | الكود يطبق Little-Endian ويحمي من التجزئة عبر `readFully`. | أن حجم الهيدر 8 بايت وأن ترتيب الحقول يطابق `ChatProtocol.cs`. |
| **المنافذ والـ IP** | التطبيق يرفض `127.0.0.1` ويطلب IP الشبكة المحلية الحقيقي. | أن المنفذ 5000 هو المنفذ المفتوح على خادم C#. |

---

## 14. خطة الاختبارات المعملية المطلوبة لإثبات التوافق (Integration Checklist)

قبل الإعلان عن اكتمال المشروع أمام الدكتور، يجب إجراء الاختبارات المعملية التالية:

1. **اختبار الطبقة الرابعة (Ping & Socket Reachability):**  
   تشغيل الخادم على الحاسوب وفحص الاتصال عبر تشغيل تطبيق الأندرويد وإدخال IP الحاسوب والتحقق من تحول الحالة إلى `Connected`.
2. **اختبار مراقبة البايتات (Network Packet Inspection):**  
   استخدام أداة مثل **Wireshark** على حاسوب الخادم لمراقبة البايتات القادمة من الهاتف عند إرسال طلب تسجيل الدخول، والتأكد من:
   - هل استقبل الخادم 8 بايتات هيدر وقرأها بنجاح؟
   - ما هو كود الرسالة الذي قرأه الخادم؟
   - هل ظهر في نافذة الخادم اتصال عميل جديد؟
3. **اختبار استجابة المصادقة:**  
   التحقق مما يرسله الخادم رداً على حزمة المصادقة ومطابقته مع دالة `handleIncomingPacket`.
4. **اختبار إرسال الرسائل المتبادل:**  
   إرسال رسالة من هاتف أندرويد والتحقق من ظهورها في شاشة `TeamWorkspaceForm.cs` على حاسوب Windows والعكس.

---

## 15. القرار الهندسي الصريح والنهائي (إجابات الأسئلة الجوهرية)

| # | السؤال الفني الحاسم | الإجابة الصريحة والقطعية |
| :---: | :--- | :--- |
| **1** | **هل التطبيق الحالي يستطيع الاتصال فعلياً بخادم CSW C#؟** | **نظرياً يستطيع فتح مقبس TCP** إن كان الـ IP والمنفذ صحيحين، **لكن إتمام المصافحة وتبادل البيانات معرض للفشل بنسبة عالية جداً** بسبب احتمالية عدم تطابق قيم `MessageType` أو حجم الهيدر. |
| **2** | **هل المصادقة مؤكدة؟** | **غير مؤكدة إطلاقاً.** لم يتم اختبارها مع الخادم الحقيقي. |
| **3** | **هل الرسائل مؤكدة؟** | **غير مؤكدة إطلاقاً.** لم يتم إرسال أو استقبال رسالة عبر الخادم بعد. |
| **4** | **هل الملفات مؤكدة؟** | **غير مؤكدة إطلاقاً.** بروتوكول نقل الملفات الحالي مبني على افتراض. |
| **5** | **هل SignalR مؤكد؟** | **غير مؤكد وغير موجود في كود أندرويد الحالي.** |
| **6** | **هل تم إجراء اختبار Integration حقيقي؟** | **لا، لم يتم إجراء أي اختبار تكامل حتى الآن.** |

---
**خلاصة القول للأستاذ والدكتور المشرف:**  
«تم إنجاز الهيكل البرمجي والمعماري الكامل لتطبيق Android Native بنجاح تام على مستوى الكود والتجميع وبناء الواجهات ومحرك المقابس؛ لكن التطبيق في وضعه الراهن يُصنف هندسياً كـ **Architectural Prototype (عميل TCP نظري غير مختبر تكاملياً)**، وتوافقه النهائي مشروط بمطابقة قيم الهيدر والتعدادات مع ملف `ChatProtocol.cs` الأصلي في معمل الاختبار».
